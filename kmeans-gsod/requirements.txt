# Pinned to the exact versions the graded run was executed and tested against,
# captured with `pip freeze` rather than written by hand.
#
#   python -m venv .venv
#   .venv\Scripts\activate          (Windows)
#   source .venv/bin/activate       (macOS / Linux)
#   pip install -r requirements.txt
#
# Verified on Python 3.13.2, Windows 11.

boto3==1.43.84
matplotlib==3.11.1
numpy==2.5.2
pandas==3.0.5
scikit-learn==1.9.0

# Resolved transitively, pinned so the environment is fully reproducible.
botocore==1.43.84
joblib==1.6.0
python-dateutil==2.9.0.post0
s3transfer==0.19.2
scipy==1.18.1
