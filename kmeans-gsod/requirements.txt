# Minimum versions rather than exact pins, so this resolves across interpreter
# versions. Python 3.9 or newer is required; 3.12+ is recommended.
#
#     python -m venv .venv
#     .venv\Scripts\activate           (Windows)
#     source .venv/bin/activate        (macOS / Linux)
#     python -m pip install --upgrade pip
#     pip install -r requirements.txt
#
# Upgrade pip first. Versions older than about 21.3 resolve dependencies poorly
# and report "no matching distribution found" when the real problem is that your
# Python is too old for the package.
#
# See requirements-lock.txt for the exact versions this was tested against, if
# you are on Python 3.11+ and want a byte-identical environment.

boto3>=1.34
matplotlib>=3.7
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
